machine:
  services:
    - docker
  java:
    version: openjdk8

dependencies:
  override:
    - echo "nothing"

test:
  pre:
    - gradle --version
    - docker version
  override:
    - gradle clean build:
        pwd: backend

deployment:
  hub:
    branch: develop
    commands:
      - docker build -t zenika/lunchplace_backend
